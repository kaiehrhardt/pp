# Automatic reveal instead of a manual host button

Classic planning poker tools let the host manually reveal the cards. We reveal automatically instead, as soon as every (non-spectator) participant has voted. This removes a click step and avoids the host becoming a bottleneck, but takes away the host's ability to deliberately delay the reveal (e.g. to nudge stragglers before uncovering the cards).
